version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --experimental-vm-modules
    build:
      commands:
        - npm run build --experimental-vm-modules
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
